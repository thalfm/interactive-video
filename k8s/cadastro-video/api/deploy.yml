apiVersion: apps/v1
kind: Deployment
metadata:
  name: cadastro-video-api
  labels:
    app: cadastro-video-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cadastro-video-api
  template:
    metadata:
      labels:
        app: cadastro-video-api
    spec:
      volumes:
        - name: nginx-api-proxy-config
          configMap:
            name: cadastro-video-api-nginx
            items:
              - key: default
                path: default
        - name: envs-api-config
          configMap:
            name: cadastro-video-api-envs
            items:
              - key: env
                path: .env
      containers:
        - name: cadastro-video-api
          image: "thalfm/cadastro-video-api"
          volumeMounts:
            - name: nginx-api-proxy-config
              mountPath: /etc/nginx/sites-available
            - name: envs-api-config
              subPath: .env
              mountPath: /opt/api/.env
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
          env:
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: pgsql-pass
                  key: password
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1024Mi"
              cpu: "2"