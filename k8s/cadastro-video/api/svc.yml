apiVersion: v1
kind: Service
metadata:
  name: "cadastro-video-api-svc"
  labels:
    app: "cadastro-video-api"
spec:
  selector:
      app: "cadastro-video-api"
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP