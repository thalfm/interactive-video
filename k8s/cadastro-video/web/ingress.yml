apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cadastro-video-web-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: cadastro-video
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cadastro-video-web-svc
                port:
                  number: 80